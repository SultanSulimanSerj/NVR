# Optional split from deploy/nginx/nvr.conf — copy to /etc/nginx/snippets/ and
# replace the inline `location /_nvr_accel/` block with:
#   include /etc/nginx/snippets/nvr-prototype-archive-accel.conf;
location /_nvr_accel/ {
    internal;
    alias /var/lib/nvr-prototype/archive/;
}
