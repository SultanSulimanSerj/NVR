# Default X-Accel archive mapping. On appliance, regenerate with:
#   nvr_sync_nginx_archive_accel.sh
# so `alias` matches archive.root_path in nvr.yaml.
location /_nvr_accel/ {
    internal;
    alias /var/lib/nvr-prototype/archive/;
}
