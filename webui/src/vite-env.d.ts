/// <reference types="vite/client" />

// Build-time constant injected by vite.config.ts. Used by the service-worker
// loader and (optionally) the UI's "About" panel.
declare const __APP_VERSION__: string;
